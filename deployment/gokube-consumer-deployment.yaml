apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: gokube-consumer
  name: gokube-consumer
spec:
  replicas: 2
  selector:
    matchLabels:
      io.kompose.service: gokube-consumer
  template:
    metadata:
      labels:
        io.kompose.network/gokube-default: "true"
        io.kompose.service: gokube-consumer
    spec:
      nodeSelector:
        tier: application
      containers:
        - name: gokube-consumer
          image: lkumbrella/gokube-consumer:latest
          env:
            - name: CRON
              value: '*/1 * * * * *'
            - name: KAFKA_ADDRESSES
              value: kafka:9093
            - name: LOGLEVEL
              value: information
            - name: MONGO_CONNECTION_STRING
              value: my-user:gokube-password@example-mongodb-0.example-mongodb-svc.default.svc.cluster.local:27017,example-mongodb-1.example-mongodb-svc.default.svc.cluster.local:27017,example-mongodb-2.example-mongodb-svc.default.svc.cluster.local:27017/admin?replicaSet=example-mongodb&ssl=false
            - name: MONGO_DATABASE
              value: gokube
          livenessProbe:
            exec:
              command:
                - curl
                - -f
                - http://localhost:8080/health
            failureThreshold: 5
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 10
          ports:
            - containerPort: 8080
              hostPort: 8100
          resources: 
            requests:
              memory: "64Mi"
            limits:
              memory: "128Mi"
              cpu: "250m"
